const doctor = require('./doctor.controller')
const description = require('./description.controller')
const menuItem = require('./menuItem.controller')
const slide = require('./slide.controller')

module.exports = {
    doctor,
    description,
    menuItem,
    slide,
}